version: '3.8'

services:
  postgres_biblioteca_docker:
    container_name: postgres_biblioteca
    image: postgres
    ports:
      - 5434:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=pg_biblioteca_db
